# Mixer4Track 複数操作の並行実行 要求仕様書

**対象**: 次期操作モデル（マスター・リミッター実装後）  
**目的**: クロスフェード、EQ変化、フェーダー、PAN、FXなど複数のパラメータ変更を、再生を止めず、音切れや不連続を起こさずに扱えるようにする。  
**状態**: 設計提案

---

## 1. 背景と問題定義

現在のMixer4Trackでは、個別の操作はリアルタイムに反映される一方、操作系は基本的に一つずつ扱う前提です。マウスで二本のフェーダーを同時に動かすことはできず、EQの複数バンドを同時に変えるためにはそれぞれを別々に操作する必要があります。また、UIスレッドからの短時間に連続した変更をそのまま音声ストリーミングへ渡すと、パラメータ更新の順序やチャンク境界によって、意図しない中間状態・クリックノイズ・処理負荷の偏りが生じる可能性があります。

本仕様では「人が物理的に複数のノブを同時に掴む」こと自体ではなく、**一つの操作が複数パラメータを同じ時刻に安全に更新できること**を並行操作と定義します。最初の対象は、DJ的なクロスフェードと、複数EQパラメータをまとめて変化させるEQ Morphです。

> **設計原則**: UI操作は意図を登録するだけとし、実際のDSPパラメータ反映は音声スレッドがチャンク境界で原子的に行う。

## 2. ユースケース

| ID | 利用シーン | 求められる操作 | 完了条件 |
|---|---|---|---|
| UC-01 | 2曲をつなぐ | 一つのX-FADERでA側を下げ、B側を上げる | 音量が滑らかに入れ替わり、無音や急なピークがない |
| UC-02 | 曲調を変える | EQ A/Bの間をMorphしてLow/Mid/Highを同時に変える | 複数バンドが同じ進行度で変化し、クリックノイズがない |
| UC-03 | 演奏中の調整 | フェーダー移動中にEQ・PAN・AUXを操作する | 最新値が次チャンクから反映され、UIが固まらない |
| UC-04 | 素早い調整 | 同じパラメータを短時間に連続操作する | 最終操作が優先され、古い中間操作で音が戻らない |
| UC-05 | 再現可能なミックス | 操作を記録して同じ時間軸で再生する | 記録した各パラメータが所定時刻に再現される |

## 3. 共通パラメータ反映モデル

### 3.1 Audio Parameter Broker

`AudioParamBroker` を追加し、UIスレッドと音声ストリーミングスレッドの間に一段の受け渡し層を置きます。UIはスライダー・ボタン・クロスフェーダーの変更をBrokerへ書き込みます。Brokerはトラックごとの最新値だけを保持し、古い中間イベントを蓄積しません。音声スレッドは各チャンクの開始時に一回だけスナップショットを取得し、そのチャンク全体に同じパラメータを適用します。

| 項目 | 仕様 |
|---|---|
| 更新単位 | 80msチャンクの開始時に最新スナップショットを取得 |
| 同一パラメータの競合 | 最終操作優先（Last Writer Wins） |
| 異なるパラメータの同時変更 | 一つのスナップショットとして同時に反映 |
| UIスレッドの責務 | 意図値の登録、表示の更新、操作履歴の記録 |
| 音声スレッドの責務 | スナップショット読込、DSPランプ適用、チャンク生成 |
| ロック方針 | 短時間のLockまたはコピーオンライト。DSP計算中はLockを保持しない |

### 3.2 変化の滑らかさ

即時に値を書き換えるとクリックノイズが出るため、連続値はランプ（補間）で遷移します。ボタンのON/OFF系も、音量に影響する場合は音声処理上で短いフェードを介して反映します。

| パラメータ種別 | 遷移方法 | 標準時間 | 補足 |
|---|---|---:|---|
| フェーダー / クロスフェーダー | 等パワーまたは線形ランプ | 10〜20ms | クロスフェードは等パワーを既定にする |
| PAN | 等パワーランプ | 10〜20ms | 左右定位を滑らかに変える |
| EQゲイン | EQEngineの既存クロスフェード | 20ms | 複数バンドは一つのEQParamsとして同時投入する |
| EQ周波数 / Q | 係数クロスフェード | 20〜40ms | 連続ドラッグ時は次チャンクごとに最新値へ合流する |
| AUX / MUTE / SOLO | ゲインランプ | 10ms | 音声の瞬断を防ぐ |
| FXプリセット | 既存FXクロスフェード | 20ms以上 | 新しい選択が来たら直前の未完遷移をキャンセルし、最新値を優先する |
| Limiter Ceiling | 短いランプ | 10ms | 急なCeiling変更による音量段差を抑える |

## 4. クロスフェーダー仕様

### 4.1 操作UI

トランスポートバーの近くに、全体で一つの`X-FADER`を追加します。各トラックは`A`、`B`、`THRU`のいずれかへ割り当てます。`THRU`はクロスフェーダーの影響を受けず、マイクや常時鳴らしたいトラック向けです。

| 操作 | 内容 |
|---|---|
| X-FADER | 左端= Aのみ、中央= A/B同量、右端= Bのみ |
| ASSIGN A | トラックをA側へ割り当てる |
| ASSIGN B | トラックをB側へ割り当てる |
| THRU | クロスフェーダーの影響を受けない |
| Curve | `Equal Power`（既定）と`Linear`を選択可能にする |
| CUT | AまたはBを即時カットするが、DSP上は10msフェードで処理する |
| SWAP | A/Bの割当を交換する |

### 4.2 等パワーカーブ

既定の`Equal Power`では、X-FADER位置を`x`（0.0〜1.0）として、A/Bのゲインを次式で求めます。

```text
gainA = cos(x × π / 2)
gainB = sin(x × π / 2)
```

中央位置でも全体の音圧が不自然に落ちにくく、音楽用途のクロスフェードに適しています。`Linear`は音量の変化を直感的に扱いたい場合に選択できます。

### 4.3 信号経路

```text
Track DSP (Gain → EQ → AUX FX)
  → Track Fader / PAN
  → X-FADER Assign Gain (A / B / THRU)
  → Master Mix Bus
  → Master GEQ
  → Master Limiter
  → Playback / REC / EXPORT WAV
```

現在のマスター合成ストリームは、全ファイルトラックを先に合成して最終出力を生成する構成です。このためX-FADERの割当ゲインは各トラックの最終ゲインへ加算するだけで、マスター・リミッター、VU、REC、EXPORT WAVへ一貫して反映できます。

## 5. EQ Morph仕様

### 5.1 目的

EQのLow/Mid/Highを個別にドラッグする代わりに、二つのEQスナップショット間を一つの`MORPH`ノブで連続変化させます。たとえば「Flat」から「Presence」へ、またはユーザーが保存したEQ AからEQ Bへ、低域・中域・高域を同時に変えられます。

### 5.2 UIと保存状態

| 要素 | 仕様 |
|---|---|
| EQ SNAP A | 現在のEQParamsをAとして保存する |
| EQ SNAP B | 現在のEQParamsをBとして保存する |
| MORPH | 0%=A、100%=B。0〜100を連続操作する |
| RESET | A側へ戻す |
| STORE | プロジェクト内にA/Bを保存する |
| 表示 | EQカーブにA/Bの薄い参照線と現在値の実線を表示する |

### 5.3 補間規則

Low/Mid/High gain、中心周波数、Qを同一の`morph`係数で線形補間し、一つの`EQParams`としてAudioParamBrokerへ登録します。DSP側では既存のEQEngineクロスフェードを使い、係数更新がクリックノイズを起こさないようにします。

```text
currentParam = eqA + (eqB - eqA) × morph
```

EQ Morphと通常のEQノブ操作が同じチャンクで発生した場合は、**最後に操作されたUIの意図が優先**されます。MORPHを掴んだ時点でEQ編集モードをMorphにし、個別ノブを動かした時点でA/Bの現在側スナップショットを更新して直接編集モードへ戻します。

## 6. 将来のオートメーション

複数操作を人の手で同時に扱うための最終形は、オートメーションです。まずPhase 24〜26でBroker、X-FADER、EQ Morphを導入し、その上で各パラメータの変更を「時刻・値・補間方式」として記録します。

| 項目 | 初期対象 | 方式 |
|---|---|---|
| Volume Automation | Track Fader、MASTER | 線形または等パワーランプ |
| Pan Automation | PAN | 等パワーランプ |
| EQ Automation | EQ Morph、各バンドgain | 係数クロスフェード |
| Switch Automation | MUTE、AUX、FX ON | 10msフェードを伴う状態切替 |
| Transport Automation | Loop、Marker Jump | 即時。ただし古いキューは安全に破棄する |

録音モードは`READ`、`WRITE`、`TOUCH`の三段階を想定します。初期実装では`READ`と`WRITE`のみ対応し、`TOUCH`は物理コントローラー対応と合わせて後続フェーズで検討します。

## 7. 例外・優先順位

| 状況 | 動作 |
|---|---|
| 同じフェーダーを連続ドラッグ | 最新値だけを使う。古いイベントをキューしない。 |
| X-FADER操作中にトラックのフェーダーを動かす | 両方のゲインを乗算する。どちらも有効。 |
| EQ Morph中にEQプリセットを選択 | プリセット選択を最後の意図として採用し、Morph遷移は終了する。 |
| FXプリセット変更が連続する | 最後に選ばれたプリセットへ合流し、古い遷移はキャンセルする。 |
| STOP / LOAD / BANK切替 | Brokerの保留状態をフラッシュし、再生開始時は最新スナップショットから開始する。 |
| LOOP境界 | EQ/FXの必要な状態をリセットしつつ、フェーダー・X-FADER・Morphの意図値は維持する。 |

## 8. 受入基準

| ID | 確認 | 合格基準 |
|---|---|---|
| CO-01 | X-FADER | A/Bの切替を10回行っても、無音・クリック・UI停止がない。 |
| CO-02 | Equal Power | 中央位置で音圧が不自然に大きく下がらない。 |
| CO-03 | EQ Morph | A→B→Aを10回繰り返しても、再生停止・明確な音切れがない。 |
| CO-04 | 並行変更 | X-FADERを動かしながらEQ MorphとPANを変えても、最終値が正しく反映される。 |
| CO-05 | REC / EXPORT | クロスフェードとEQ Morph中に録音したWAVが、再生時と同じ変化を含む。 |
| CO-06 | 負荷 | 8トラック再生中に複数操作を行っても、音声ストリームのキュー枯渇が起きない。 |

## 9. 実装ロードマップ

| Phase | 実装内容 | 依存 |
|---|---|---|
| Phase 24 | `AudioParamBroker`、パラメータスナップショット、ゲインランプ | 現行マスター合成ストリーム |
| Phase 25 | X-FADER、A/B/THRU割当、Equal Power / Linearカーブ | Phase 24 |
| Phase 26 | EQ Snap A/B、EQ Morph、カーブ表示 | Phase 24 |
| Phase 27 | オートメーション記録・再生（Volume/PANから開始） | Phase 24〜26 |
| Phase 28 | MIDI Learn、タッチ／物理コントローラー向け操作モデル | Phase 27 |

## 10. 今回の意思決定

最初に実装する並行操作は**Phase 24のAudioParamBroker**と**Phase 25のX-FADER**を推奨します。これにより二本のフェーダーを同時に動かす必要を、一つの明確な操作へ置き換えられます。EQ Morphは同じBroker基盤を再利用でき、音作りの同時操作として自然に次の段階へ進めます。

---

*作成: Manus AI*  
*本仕様は現行アーキテクチャを基にした設計提案であり、実装前にUIモックと実機音声テストで細部を確認する。*
